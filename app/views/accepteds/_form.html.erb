<%= form_with(model: accepted, local: true) do |form| %>
  <% if accepted.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accepted.errors.count, "error") %> prohibited this accepted from being saved:</h2>

      <ul>
      <% accepted.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id, id: :accepted_event_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :accepted_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
